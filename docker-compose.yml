services:
  paint-index:
    image: ghcr.io/diptamahardhika/paint-index:latest
    container_name: paint-index-app
    pull_policy: always
    ports:
      - "8081:80"
    restart: unless-stopped
    volumes:
      - /opt/paint-index/firebase.js:/usr/share/nginx/html/firebase.js:ro
      
